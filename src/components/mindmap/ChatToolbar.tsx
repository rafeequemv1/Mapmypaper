
import React from "react";

const ChatToolbar: React.FC = () => {
  return (
    <div className="flex gap-2 px-2 py-1 border-b bg-white items-center">
      {/* Toolbar is now empty */}
    </div>
  );
};

export default ChatToolbar;
